CREATE TABLE cursos (
                         id BIGINT not null auto_increment,
                         nombre VARCHAR(100) not null,
                         categoria VARCHAR(100) not null,

                         primary key(id)
);